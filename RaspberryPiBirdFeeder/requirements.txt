# Raspberry Pi Bird Feeder Detection System Requirements
# Install with: pip install -r requirements.txt

# Core dependencies
numpy>=1.19.0
Pillow>=8.0.0
PyYAML>=5.4

# Camera support
# For Raspberry Pi Camera Module (newer OS versions)
picamera2>=0.3.9; platform_machine == "aarch64" or platform_machine == "armv7l"

# For legacy Raspberry Pi Camera support (older OS versions)
# picamera>=1.13; platform_machine == "armv7l"

# OpenCV for image processing and motion detection
opencv-python-headless>=4.5.0

# TensorFlow Lite for ML inference
# Choose ONE of the following based on your setup:

# Option 1: TFLite Runtime (recommended - smaller, faster)
tflite-runtime>=2.10.0

# Option 2: Full TensorFlow (if you need training capabilities)
# tensorflow>=2.10.0

# Notification services
twilio>=7.0.0

# HTTP requests (optional, for some notification services)
requests>=2.25.0

# Web interface dependencies (optional)
# flask>=2.0.0
# flask-cors>=3.0.10

# Development and testing (optional)
# pytest>=6.0.0
# pytest-cov>=2.0.0
